// nandina_controls_module.cpp
// Minimal translation unit that ensures NandinaControls is built as a proper
// shared library rather than an INTERFACE/QML-only target.  This lets the
// CMake generator emit an absolute-path linker argument instead of -lNandinaControls.
